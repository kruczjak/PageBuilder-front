import { CodeMirrorDirective } from './code-mirror.directive';

describe('CodeMirrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeMirrorDirective();
    expect(directive).toBeTruthy();
  });
});
